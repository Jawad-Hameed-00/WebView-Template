name: Android App Builder (Signed APK)

on:
  workflow_dispatch:
    inputs:
      build_url:
        description: 'URL to build.json'
        required: true
        default: 'https://savist.online/build.json'

env:
  BUILD_CONFIG_URL: ${{ github.event.inputs.build_url }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # --------------------------------------------------
    # Checkout repository
    # --------------------------------------------------
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: master

    # --------------------------------------------------
    # Set up JDK
    # --------------------------------------------------
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'

    # --------------------------------------------------
    # Download build configuration
    # --------------------------------------------------
    - name: Download build configuration
      run: |
        echo "Downloading build config from: $BUILD_CONFIG_URL"
        curl -s -o build_config.json "$BUILD_CONFIG_URL"
        echo "Build config:"
        cat build_config.json

    # --------------------------------------------------
    # Parse JSON configuration
    # --------------------------------------------------
    - name: Parse build configuration
      id: config
      run: |
        echo "Extracting build parameters..."
        
        WEBSITE_URL=$(jq -r '.website_url' build_config.json)
        APP_NAME=$(jq -r '.app_name' build_config.json)
        PACKAGE_NAME=$(jq -r '.package_name' build_config.json)
        LOGO_URL=$(jq -r '.logo_url' build_config.json)
        USER_ID=$(jq -r '.user_id' build_config.json)
        
        echo "APP_NAME=$APP_NAME" >> $GITHUB_OUTPUT
        echo "PACKAGE_NAME=$PACKAGE_NAME" >> $GITHUB_OUTPUT
        echo "WEBSITE_URL=$WEBSITE_URL" >> $GITHUB_OUTPUT
        echo "LOGO_URL=$LOGO_URL" >> $GITHUB_OUTPUT
        echo "USER_ID=$USER_ID" >> $GITHUB_OUTPUT
        
        echo "App Name: $APP_NAME"
        echo "Package: $PACKAGE_NAME"
        echo "Website: $WEBSITE_URL"

    # --------------------------------------------------
    # Find original package name (Supports Kotlin DSL)
    # --------------------------------------------------
    - name: Find original package name
      id: find_package
      run: |
        echo "Finding original package name..."
        
        # Try AndroidManifest.xml first
        if [ -f "app/src/main/AndroidManifest.xml" ]; then
          ORIGINAL_PACKAGE=$(grep -o 'package="[^"]*"' app/src/main/AndroidManifest.xml | head -1 | sed 's/package="//' | sed 's/"//')
          echo "Found in AndroidManifest.xml: $ORIGINAL_PACKAGE"
        elif [ -f "app/build.gradle.kts" ]; then
          # Try Kotlin DSL build.gradle.kts
          ORIGINAL_PACKAGE=$(grep -o "namespace = [\"'][^\"']*[\"']" app/build.gradle.kts | head -1 | sed 's/namespace = //' | sed 's/["'\'']//g' || 
                            grep -o 'applicationId = "[^"]*"' app/build.gradle.kts | head -1 | sed 's/applicationId = "//' | sed 's/"//')
          echo "Found in build.gradle.kts: $ORIGINAL_PACKAGE"
        elif [ -f "app/build.gradle" ]; then
          # Try Groovy DSL build.gradle
          ORIGINAL_PACKAGE=$(grep -o "namespace ['\"][^'\"]*['\"]" app/build.gradle | head -1 | sed "s/namespace ['\"]//" | sed "s/['\"]//" || 
                            grep -o 'applicationId "[^"]*"' app/build.gradle | head -1 | sed 's/applicationId "//' | sed 's/"//')
          echo "Found in build.gradle: $ORIGINAL_PACKAGE"
        else
          # Find from Java/Kotlin files
          JAVA_FILE=$(find app/src/main/java -name "*.java" -o -name "*.kt" 2>/dev/null | head -1)
          if [ -f "$JAVA_FILE" ]; then
            ORIGINAL_PACKAGE=$(grep -o 'package [^;]*' "$JAVA_FILE" | head -1 | sed 's/package //')
            echo "Found in Java file: $ORIGINAL_PACKAGE"
          else
            ORIGINAL_PACKAGE="com.yourapp.app"
            echo "Using default: $ORIGINAL_PACKAGE"
          fi
        fi
        
        echo "ORIGINAL_PACKAGE=$ORIGINAL_PACKAGE" >> $GITHUB_OUTPUT
        echo "NEW_PACKAGE=${{ steps.config.outputs.PACKAGE_NAME }}" >> $GITHUB_OUTPUT

    # --------------------------------------------------
    # Setup Android SDK
    # --------------------------------------------------
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    # --------------------------------------------------
    # Create keystore
    # --------------------------------------------------
    - name: Create release keystore
      run: |
        echo "Creating keystore..."
        keytool -genkeypair \
          -keystore release.jks \
          -alias release \
          -keyalg RSA \
          -keysize 2048 \
          -validity 10000 \
          -storepass android123 \
          -keypass android123 \
          -dname "CN=AppBuilder, OU=Mobile, O=Savist, L=Bahawalpur, S=Punjab, C=PK"
        echo "Keystore created"

    # --------------------------------------------------
    # Configure signing for Kotlin DSL
    # --------------------------------------------------
    - name: Configure signing
      run: |
        echo "Configuring signing..."
        
        # Add to gradle.properties (works for both Groovy and Kotlin DSL)
        echo "" >> gradle.properties
        echo "# Auto-generated signing config" >> gradle.properties
        echo "RELEASE_STORE_FILE=$(pwd)/release.jks" >> gradle.properties
        echo "RELEASE_STORE_PASSWORD=android123" >> gradle.properties
        echo "RELEASE_KEY_ALIAS=release" >> gradle.properties
        echo "RELEASE_KEY_PASSWORD=android123" >> gradle.properties
        
        # Also add to local.properties
        echo "" >> local.properties 2>/dev/null || echo "" > local.properties
        echo "# Auto-generated signing config" >> local.properties
        echo "RELEASE_STORE_FILE=$(pwd)/release.jks" >> local.properties
        echo "RELEASE_STORE_PASSWORD=android123" >> local.properties
        echo "RELEASE_KEY_ALIAS=release" >> local.properties
        echo "RELEASE_KEY_PASSWORD=android123" >> local.properties

    # --------------------------------------------------
    # Download and update app logo
    # --------------------------------------------------
    - name: Download and update app logo
      run: |
        echo "Downloading logo from: ${{ steps.config.outputs.LOGO_URL }}"
        
        # Install imagemagick for image processing
        sudo apt-get update
        sudo apt-get install -y imagemagick
        
        # Download logo
        wget -O downloaded_logo.png "${{ steps.config.outputs.LOGO_URL }}"
        
        # Update existing launcher icons
        find app/src/main/res -name "ic_launcher*.png" -o -name "ic_launcher*.jpg" -o -name "ic_launcher*.jpeg" -o -name "ic_launcher*.webp" 2>/dev/null | while read icon; do
          echo "Updating: $icon"
          dir=$(dirname "$icon")
          
          # Resize based on directory
          case "$dir" in
            *mipmap-hdpi*)
              convert downloaded_logo.png -resize 72x72 "$icon"
              ;;
            *mipmap-mdpi*)
              convert downloaded_logo.png -resize 48x48 "$icon"
              ;;
            *mipmap-xhdpi*)
              convert downloaded_logo.png -resize 96x96 "$icon"
              ;;
            *mipmap-xxhdpi*)
              convert downloaded_logo.png -resize 144x144 "$icon"
              ;;
            *mipmap-xxxhdpi*)
              convert downloaded_logo.png -resize 192x192 "$icon"
              ;;
            *)
              # Default size
              convert downloaded_logo.png -resize 512x512 "$icon"
              ;;
          esac
        done

    # --------------------------------------------------
    # FIRST: Create strings.xml BEFORE any modifications
    # --------------------------------------------------
    - name: Create strings.xml
      run: |
        echo "=== CREATING STRINGS.XML ==="
        
        # Create directory
        mkdir -p app/src/main/res/values
        
        # Use printf for safer string handling
        printf '<?xml version="1.0" encoding="utf-8"?>\n' > app/src/main/res/values/strings.xml
        printf '<resources>\n' >> app/src/main/res/values/strings.xml
        printf '    <string name="app_name">%s</string>\n' "${{ steps.config.outputs.APP_NAME }}" >> app/src/main/res/values/strings.xml
        printf '    <string name="website_url">%s</string>\n' "${{ steps.config.outputs.WEBSITE_URL }}" >> app/src/main/res/values/strings.xml
        printf '</resources>\n' >> app/src/main/res/values/strings.xml
        
        echo "Created strings.xml with:"
        echo "App Name: ${{ steps.config.outputs.APP_NAME }}"
        echo "Website: ${{ steps.config.outputs.WEBSITE_URL }}"
        
        echo "File content (first 5 lines):"
        head -5 app/src/main/res/values/strings.xml
        echo "=== END STRINGS.XML ==="

    # --------------------------------------------------
    # COMPLETE PACKAGE RENAME FOR KOTLIN DSL
    # --------------------------------------------------
    - name: Complete Package Rename for Kotlin DSL
      run: |
        echo "=== PACKAGE RENAME PROCESS FOR KOTLIN DSL ==="
        OLD_PACKAGE="${{ steps.find_package.outputs.ORIGINAL_PACKAGE }}"
        NEW_PACKAGE="${{ steps.config.outputs.PACKAGE_NAME }}"
        
        echo "From: $OLD_PACKAGE"
        echo "To: $NEW_PACKAGE"
        
        # Convert package names to directory paths
        OLD_PATH=$(echo "$OLD_PACKAGE" | tr '.' '/')
        NEW_PATH=$(echo "$NEW_PACKAGE" | tr '.' '/')
        
        echo "Old path: $OLD_PATH"
        echo "New path: $NEW_PATH"
        
        # Step 1: Update AndroidManifest.xml
        echo "1. Updating AndroidManifest.xml..."
        if [ -f "app/src/main/AndroidManifest.xml" ]; then
          # Backup original
          cp app/src/main/AndroidManifest.xml app/src/main/AndroidManifest.xml.backup
          
          # Update package attribute
          sed -i "s|package=\"$OLD_PACKAGE\"|package=\"$NEW_PACKAGE\"|g" app/src/main/AndroidManifest.xml
          
          # Update activity references
          sed -i "s|android:name=\"\\.|android:name=\"$NEW_PACKAGE\\.|g" app/src/main/AndroidManifest.xml
          sed -i "s|android:name=\"$OLD_PACKAGE\\.|android:name=\"$NEW_PACKAGE\\.|g" app/src/main/AndroidManifest.xml
          
          echo "✓ AndroidManifest.xml updated"
        fi
        
        # Step 2: Update ALL Gradle files (both .gradle and .gradle.kts)
        echo "2. Updating Gradle files..."
        
        # Update app/build.gradle.kts if it exists
        if [ -f "app/build.gradle.kts" ]; then
          echo "  Updating: app/build.gradle.kts"
          
          # Backup
          cp app/build.gradle.kts app/build.gradle.kts.backup
          
          # Update namespace (Kotlin DSL format)
          sed -i "s|namespace = \"$OLD_PACKAGE\"|namespace = \"$NEW_PACKAGE\"|g" app/build.gradle.kts
          sed -i "s|namespace = '$OLD_PACKAGE'|namespace = \"$NEW_PACKAGE\"|g" app/build.gradle.kts
          
          # Update applicationId (Kotlin DSL format)
          sed -i "s|applicationId = \"$OLD_PACKAGE\"|applicationId = \"$NEW_PACKAGE\"|g" app/build.gradle.kts
          sed -i "s|applicationId = '$OLD_PACKAGE'|applicationId = \"$NEW_PACKAGE\"|g" app/build.gradle.kts
          
          echo "  ✓ app/build.gradle.kts updated"
        fi
        
        # Update app/build.gradle if it exists (Groovy DSL)
        if [ -f "app/build.gradle" ]; then
          echo "  Updating: app/build.gradle"
          cp app/build.gradle app/build.gradle.backup
          sed -i "s|namespace \"$OLD_PACKAGE\"|namespace \"$NEW_PACKAGE\"|g" app/build.gradle
          sed -i "s|namespace '$OLD_PACKAGE'|namespace \"$NEW_PACKAGE\"|g" app/build.gradle
          sed -i "s|applicationId \"$OLD_PACKAGE\"|applicationId \"$NEW_PACKAGE\"|g" app/build.gradle
          sed -i "s|applicationId '$OLD_PACKAGE'|applicationId \"$NEW_PACKAGE\"|g" app/build.gradle
          echo "  ✓ app/build.gradle updated"
        fi
        
        # Update other Gradle files
        find . -name "*.gradle" -o -name "*.gradle.kts" | grep -v "build/" | while read gradle_file; do
          if [[ "$gradle_file" != *".backup" ]]; then
            echo "  Updating other: $gradle_file"
            # Update any package references (be careful not to match version numbers)
            sed -i "s|\"$OLD_PACKAGE\"|\"$NEW_PACKAGE\"|g" "$gradle_file"
            sed -i "s|'$OLD_PACKAGE'|'$NEW_PACKAGE'|g" "$gradle_file"
          fi
        done
        
        echo "✓ All Gradle files updated"
        
        # Step 3: Update source files
        echo "3. Updating source files..."
        
        # Process Kotlin files first
        if [ -d "app/src/main/kotlin" ]; then
          echo "  Updating Kotlin files..."
          find app/src/main/kotlin -type f -name "*.kt" | while read kt_file; do
            echo "    Updating: $kt_file"
            # Update package declaration
            sed -i "s|^package $OLD_PACKAGE$|package $NEW_PACKAGE|g" "$kt_file"
            sed -i "s|^package $OLD_PACKAGE |package $NEW_PACKAGE |g" "$kt_file"
            # Update imports
            sed -i "s|import $OLD_PACKAGE\\.|import $NEW_PACKAGE\\.|g" "$kt_file"
          done
          echo "  ✓ Kotlin files updated"
        fi
        
        # Process Java files
        if [ -d "app/src/main/java" ]; then
          echo "  Updating Java files..."
          find app/src/main/java -type f -name "*.java" | while read java_file; do
            echo "    Updating: $java_file"
            # Update package declaration
            sed -i "s|^package $OLD_PACKAGE;|package $NEW_PACKAGE;|g" "$java_file"
            # Update imports
            sed -i "s|import $OLD_PACKAGE\\.|import $NEW_PACKAGE\\.|g" "$java_file"
          done
          echo "  ✓ Java files updated"
        fi
        
        # Step 4: Move source files to new package structure
        echo "4. Moving source files..."
        
        # Move Kotlin files
        if [ -d "app/src/main/kotlin/$OLD_PATH" ] && [ "$OLD_PACKAGE" != "$NEW_PACKAGE" ]; then
          echo "  Moving Kotlin from: $OLD_PATH to $NEW_PATH"
          mkdir -p "app/src/main/kotlin/$NEW_PATH"
          
          # Move all files and directories
          find "app/src/main/kotlin/$OLD_PATH" -maxdepth 1 -type f -exec mv {} "app/src/main/kotlin/$NEW_PATH/" \; 2>/dev/null || true
          find "app/src/main/kotlin/$OLD_PATH" -maxdepth 1 -type d ! -name "$OLD_PATH" -exec mv {} "app/src/main/kotlin/$NEW_PATH/" \; 2>/dev/null || true
          
          # Remove old directory if empty
          rmdir "app/src/main/kotlin/$OLD_PATH" 2>/dev/null || true
        fi
        
        # Move Java files
        if [ -d "app/src/main/java/$OLD_PATH" ] && [ "$OLD_PACKAGE" != "$NEW_PACKAGE" ]; then
          echo "  Moving Java from: $OLD_PATH to $NEW_PATH"
          mkdir -p "app/src/main/java/$NEW_PATH"
          
          find "app/src/main/java/$OLD_PATH" -maxdepth 1 -type f -exec mv {} "app/src/main/java/$NEW_PATH/" \; 2>/dev/null || true
          find "app/src/main/java/$OLD_PATH" -maxdepth 1 -type d ! -name "$OLD_PATH" -exec mv {} "app/src/main/java/$NEW_PATH/" \; 2>/dev/null || true
          
          rmdir "app/src/main/java/$OLD_PATH" 2>/dev/null || true
        fi
        
        echo "✓ Source files moved"
        
        # Step 5: Update settings.gradle.kts
        echo "5. Updating settings.gradle.kts..."
        if [ -f "settings.gradle.kts" ]; then
          echo "  Found settings.gradle.kts"
          # Update rootProject.name
          sed -i "s|rootProject.name =.*|rootProject.name = \"${{ steps.config.outputs.APP_NAME }}\"|g" settings.gradle.kts
          echo "  ✓ settings.gradle.kts updated"
        elif [ -f "settings.gradle" ]; then
          echo "  Found settings.gradle"
          sed -i "s|rootProject.name =.*|rootProject.name = \"${{ steps.config.outputs.APP_NAME }}\"|g" settings.gradle
          echo "  ✓ settings.gradle updated"
        fi
        
        echo "=== PACKAGE RENAME COMPLETE ==="

    # --------------------------------------------------
    # Clean up backup files
    # --------------------------------------------------
    - name: Clean backup files
      run: |
        echo "Cleaning up backup files..."
        find . -type f \( -name "*.backup" -o -name "*.bak" -o -name "*.back" -o -name "*~" \) -delete 2>/dev/null || true

    # --------------------------------------------------
    # Debug: Show what we've changed
    # --------------------------------------------------
    - name: Debug changes
      run: |
        echo "=== DEBUG: Checking critical files ==="
        echo ""
        echo "1. strings.xml:"
        echo "---------------------------------"
        cat app/src/main/res/values/strings.xml
        echo "---------------------------------"
        echo ""
        echo "2. AndroidManifest.xml package:"
        echo "---------------------------------"
        grep 'package=' app/src/main/AndroidManifest.xml 2>/dev/null || echo "Not found"
        echo ""
        echo "3. Gradle file applicationId/namespace:"
        echo "---------------------------------"
        if [ -f "app/build.gradle.kts" ]; then
          echo "Using Kotlin DSL:"
          grep -i -E "(namespace|applicationId)" app/build.gradle.kts 2>/dev/null || echo "Not found"
        elif [ -f "app/build.gradle" ]; then
          echo "Using Groovy DSL:"
          grep -i -E "(namespace|applicationId)" app/build.gradle 2>/dev/null || echo "Not found"
        fi
        echo ""
        echo "4. Directory structure:"
        echo "---------------------------------"
        find app/src/main/kotlin -type f -name "*.kt" 2>/dev/null | head -10 || echo "No Kotlin files found"
        find app/src/main/java -type f -name "*.java" 2>/dev/null | head -10 || echo "No Java files found"

    # --------------------------------------------------
    # Clean and build
    # --------------------------------------------------
    - name: Clean and Build APK
      run: |
        echo "=== CLEANING AND BUILDING APK ==="
        
        # Clean
        echo "Cleaning..."
        rm -rf app/build .gradle build 2>/dev/null || true
        
        # Make gradlew executable
        if [ -f "gradlew" ]; then
          chmod +x gradlew
        else
          echo "Initializing gradle wrapper..."
          gradle wrapper --gradle-version 8.0
          chmod +x gradlew
        fi
        
        # Build with debug info
        echo "Building release APK..."
        ./gradlew clean assembleRelease --stacktrace
        
        echo "=== BUILD COMPLETED ==="
        
        # Show APK files
        echo "Generated APK files:"
        find . -name "*.apk" -type f | while read file; do
          echo "  - $file ($(ls -lh "$file" | awk '{print $5}'))"
        done

    # --------------------------------------------------
    # Verify APK
    # --------------------------------------------------
    - name: Verify APK
      run: |
        echo "=== VERIFYING APK ==="
        
        APK_FILE=$(find app/build/outputs/apk -name "*release*.apk" -type f | head -1)
        
        if [ -f "$APK_FILE" ]; then
          echo "✓ APK found: $APK_FILE"
          echo "  Size: $(ls -lh "$APK_FILE" | awk '{print $5}')"
          
          # Try to get package info
          if command -v aapt &> /dev/null || command -v aapt2 &> /dev/null; then
            echo ""
            echo "APK Package Info:"
            echo "-----------------"
            if command -v aapt &> /dev/null; then
              aapt dump badging "$APK_FILE" | grep -E "(package|application-label)" || true
            elif command -v aapt2 &> /dev/null; then
              aapt2 dump badging "$APK_FILE" | grep -E "(package|application-label)" || true
            fi
            echo "-----------------"
          fi
        else
          echo "✗ ERROR: No APK file found!"
          exit 1
        fi
        echo "=== VERIFICATION COMPLETE ==="

    # --------------------------------------------------
    # Upload APK
    # --------------------------------------------------
    - name: Upload Signed APK
      uses: actions/upload-artifact@v4
      with:
        name: ${{ steps.config.outputs.APP_NAME }}-${{ steps.config.outputs.USER_ID }}
        path: app/build/outputs/apk/**/*.apk
        retention-days: 7

    # --------------------------------------------------
    # Completion
    # --------------------------------------------------
    - name: Notify completion
      run: |
        echo "✅ Build completed successfully!"
        echo "📱 App Name: ${{ steps.config.outputs.APP_NAME }}"
        echo "📦 Package: ${{ steps.config.outputs.PACKAGE_NAME }}"
        echo "🌐 Website: ${{ steps.config.outputs.WEBSITE_URL }}"
        echo "👤 User ID: ${{ steps.config.outputs.USER_ID }}"
